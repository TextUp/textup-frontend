<img src="{{_previewUrl}}" alt="{{_previewAlt}}">

{{#if _isShowingAddress}}
    {{location.address}}
{{else}}
    {{#if _isLoading}}
        {{loadingMessage}}
    {{else if _isError}}
        {{errorMessage}}
    {{/if}}
{{/if}}
