{{#hide-away doRegister=(action (mut _hideAway))
	tabindex=false
	animation='none'
    triggerClass='text-ellipsis'
	bodyClass='text-ellipsis'
	hideTriggerOnOpen=true}}
	{{content}}
	{{yield}}
{{else}}
    {{abbreviated}}
    {{yield to='inverse'}}
{{/hide-away}}
