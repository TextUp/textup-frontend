{{#liquid-spacer}}
    {{yield}}
    {{#if images}}
        {{photo-control classNames="record-actions-control__media-drawer"
            images=images
            imageDisplayComponent=constants.PHOTO_CONTROL.DISPLAY.GRID
            onRemove=(action _onRemoveImage)}}
    {{/if}}
{{/liquid-spacer}}
