<p class="text-ellipsis">
    {{yield}}
</p>
{{#if showCloseButton}}
    <button type="button" class="btn btn-plain" onclick={{optional-action onClose}}>
        {{closeButtonLabel}}
    </button>
{{/if}}
