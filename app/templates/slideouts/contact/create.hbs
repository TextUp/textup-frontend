{{#slideout-pane
    headerComponent=(component "slideout-pane/title" title="New Contact")
    footerComponent=(component "slideout-pane/actions"
        showFooter=true
        onPrimary=(service-action "newContactSlideoutService" "finishSlideout")
        disablePrimary=(service-prop "newContactSlideoutService" "newContact.validations.isInvalid")
        primaryLabel="Create"
        primaryProgressLabel="Creating...")
    onClose=(service-action "newContactSlideoutService" "cancelSlideout")
    forceKeepOpen=(service-prop "newContactSlideoutService" "newContact.isSaving")
    focusSelector=".start-focus" as |slideout|}}
    {{model-form/contact firstControlClass="start-focus"
        contact=(service-prop "newContactSlideoutService" "newContact")
        onGoToDuplicates=(service-action "newContactSlideoutService" "goToDuplicate")
        onChangeNumbers=(service-mut "newContactSlideoutService" "newContact.numbers")
        onAddNumber=(service-action "contactService" "checkNumberDuplicate"
            (service-prop "newContactSlideoutService" "newContact"))
        onRemoveNumber=(service-action "contactService" "removeNumberDuplicate"
            (service-prop "newContactSlideoutService" "newContact"))}}
{{/slideout-pane}}
