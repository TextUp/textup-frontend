{{#if (and (not team.hasInactivePhone)
    team.phone.isFulfilled
    team.phone.content)}}
    <span class="badge">
        {{#tool-tip tagName='span'
            side=constants.TOOLTIP.SIDE.RIGHT
            title=(concat 'TextUp Phone ' (phone-number team.phone.content.number))}}
            <span class="fa fa-phone"></span>
        {{/tool-tip}}
    </span>
{{/if}}
{{#if team.hasInactivePhone}}
    <span class="badge">
        {{#tool-tip tagName='span'
            class='text-lighten'
            side=constants.TOOLTIP.SIDE.RIGHT
            title='Has inactive phone'}}
            <span class="fa fa-phone"></span>
        {{/tool-tip}}
    </span>
{{/if}}
<span class="badge">
    {{team.numMembers}}
    <span class="fa fa-user"></span>
</span>
