import { config } from 'textup-frontend/helpers/config';
import { module, test } from 'qunit';

module('Unit | Helper | config');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = config([42]);
  assert.ok(result);
});
