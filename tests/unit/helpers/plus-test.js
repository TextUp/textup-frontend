import { plus } from 'textup-frontend/helpers/plus';
import { module, test } from 'qunit';

module('Unit | Helper | plus');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = plus([42]);
  assert.ok(result);
});
