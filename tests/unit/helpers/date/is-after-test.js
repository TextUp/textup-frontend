import { dateIsAfter } from 'textup-frontend/helpers/date/is-after';
import { module, test } from 'qunit';

module('Unit | Helper | date/is after');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateIsAfter([42]);
  assert.ok(result);
});
