import { dateIsSameDay } from 'textup-frontend/helpers/date/is-same-day';
import { module, test } from 'qunit';

module('Unit | Helper | date/is same day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateIsSameDay([42]);
  assert.ok(result);
});
