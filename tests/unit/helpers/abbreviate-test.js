import { abbreviate } from 'textup-frontend/helpers/abbreviate';
import { module, test } from 'qunit';

module('Unit | Helper | abbreviate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = abbreviate([42]);
  assert.ok(result);
});
