import coordinate from 'textup-frontend/utils/coordinate';
import { module, test } from 'qunit';

module('Unit | Utility | coordinate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coordinate();
  assert.ok(result);
});
