import property from 'textup-frontend/utils/property';
import { module, test } from 'qunit';

module('Unit | Utility | property');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = property();
  assert.ok(result);
});
