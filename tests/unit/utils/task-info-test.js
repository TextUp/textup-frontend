import taskInfo from 'textup-frontend/utils/task-info';
import { module, test } from 'qunit';

module('Unit | Utility | task info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskInfo();
  assert.ok(result);
});
