import appAccess from 'textup-frontend/utils/app-access';
import { module, test } from 'qunit';

module('Unit | Utility | app access');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = appAccess();
  assert.ok(result);
});
