import color from 'textup-frontend/utils/color';
import { module, test } from 'qunit';

module('Unit | Utility | color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = color();
  assert.ok(result);
});
