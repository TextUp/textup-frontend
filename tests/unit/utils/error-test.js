import error from 'textup-frontend/utils/error';
import { module, test } from 'qunit';

module('Unit | Utility | error');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = error();
  assert.ok(result);
});
