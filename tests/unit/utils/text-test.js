import text from 'textup-frontend/utils/text';
import { module, test } from 'qunit';

module('Unit | Utility | text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = text();
  assert.ok(result);
});
