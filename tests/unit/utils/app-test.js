import app from 'textup-frontend/utils/app';
import { module, test } from 'qunit';

module('Unit | Utility | app');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = app();
  assert.ok(result);
});
