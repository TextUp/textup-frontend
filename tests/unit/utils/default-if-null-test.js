import defaultIfNull from 'textup-frontend/utils/default-if-null';
import { module, test } from 'qunit';

module('Unit | Utility | default if null');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = defaultIfNull();
  assert.ok(result);
});
