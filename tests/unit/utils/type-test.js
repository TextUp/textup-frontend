import type from 'textup-frontend/utils/type';
import { module, test } from 'qunit';

module('Unit | Utility | type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = type();
  assert.ok(result);
});
