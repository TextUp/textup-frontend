import schedule from 'textup-frontend/utils/schedule';
import { module, test } from 'qunit';

module('Unit | Utility | schedule');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = schedule();
  assert.ok(result);
});
