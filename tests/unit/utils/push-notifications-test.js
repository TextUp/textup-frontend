import pushNotifications from 'textup-frontend/utils/push-notifications';
import { module, test } from 'qunit';

module('Unit | Utility | push notifications');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pushNotifications();
  assert.ok(result);
});
